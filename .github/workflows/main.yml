name: frist workflow

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: print message
        run: echo "welcome github action"
      

  
