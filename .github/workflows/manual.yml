on:
  workflow_dispatch:
    inputs:
      environment:
        description: "choose environment"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - prouction

jobs:
  deploy:
     runs-on: ubuntu-latest
     steps:
       - name: deploy app
         run: echo "deploy the app to ${{ inputs.environment}}"
